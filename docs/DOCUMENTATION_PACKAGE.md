# 🏗 Construction AI System — Проектная Документация

**Версия:** 4.0 (Enterprise Demo)
**Дата:** 22.11.2025
**Автор:** [Твое Имя] (Prompt Engineer / AI Architect)

---

## 1. Описание проекта (Executive Summary)

**Construction AI System** — это интеллектуальная платформа автоматизации клиентского сервиса для строительного ритейла (на примере "Строительный Двор").

### Проблема:
*   Высокая нагрузка на колл-центр и менеджеров.
*   Человеческий фактор: грубость, ошибки, долгое время реакции.
*   Отсутствие быстрой аналитики по жалобам ("почему клиенты уходят?").

### Решение:
Внедрение AI-агента (Telegram Bot), который берет на себя рутину 24/7:
1.  **Фильтрует поток:** Мгновенно отделяет "Лиды" (Продажи) от "Жалоб" и "Спама".
2.  **Гасит конфликты:** Генерирует эмпатичные, профессиональные ответы на жалобы, защищая репутацию бренда.
3.  **Помогает клиентам:** Распознает товары и поломки по фото (Computer Vision).
4.  **Дает аналитику:** Превращает 1000 строк Excel в понятные графики за 30 секунд.

**Бизнес-эффект:** Снижение нагрузки на ФОТ до 40%, рост скорости ответа в 10 раз, повышение NPS (лояльности).

---

## 2. Техническая Архитектура (Architecture)

Система построена как отказоустойчивый микросервис.

### Стек технологий:
*   **Язык:** Python 3.10 (AsyncIO).
*   **Интерфейс:** Python-Telegram-Bot (v20+).
*   **AI Orchestration:** Гибридная схема (Failover).
    *   *Primary:* **OpenRouter** (DeepSeek R1 / Llama 3 / Gemini Flash) — для экономии.
    *   *Fallback:* **Google Gemini 2.0 Flash (Direct API)** — для надежности (SLA 99.9%).
*   **Data Pipeline (ETL):** Pandas (обработка CSV), Matplotlib (генерация инфографики в RAM).
*   **Deployment:** Docker Container (Alpine Linux).

### Схема работы (Logic Flow):
1.  **Input:** Текст / Фото / Файл (.csv).
2.  **Router:** Определяет тип задачи.
3.  **AI Core:**
    *   Если Текст -> `Analyst Agent` (JSON Classification).
    *   Если Жалоба -> `Support Agent` (Draft Generation).
    *   Если Фото -> `Vision Agent` (Image Recognition).
4.  **Output:** Готовый ответ, Отчет (CSV) или Дашборд (JPG).

---

## 3. Промпт-Инжиниринг (Secret Sauce)

В проекте используются продвинутые техники управления LLM:

### A. Structured Output (JSON Mode)
Мы не просим модель "просто ответить". Мы заставляем её возвращать строгий JSON:
```json
{
  "intent": "complaint",
  "urgency": "high",
  "sentiment": "negative"
}
```
Это позволяет интегрировать AI с базой данных и CRM.

### B. Negative Constraints (Brand Safety)
В системный промт `Support Agent` внедрены жесткие ограничения:
> ⛔️ **ЗАПРЕЩЕНО:** Использовать фразы самоуничижения ("мы облажались", "мы не соответствуем стандартам").
> ✅ **РАЗРЕШЕНО:** Признавать локальный сбой и предлагать решение.

Это предотвращает репутационные риски, когда "глупый" бот берет на себя вину за всё подряд.

### C. Few-Shot Learning & Context
Промты адаптированы под специфику "Строительного Двора": модель знает про "Клуб Профи", "Манипуляторы" и бренды (Knauf, Ceresit).

---

## 4. Руководство пользователя (User Guide)

### Как начать работу:
1.  Откройте бота в Telegram.
2.  Нажмите `/start`.
3.  Используйте меню внизу экрана.

### Сценарии:
*   **Анализ CSV:**
    1. Нажмите `[📂 Скачать пример CSV]`.
    2. Отправьте этот (или свой) файл боту.
    3. Подождите ~30 секунд.
    4. Получите отчет и графики.
*   **Экспертиза по фото:**
    1. Нажмите скрепку -> Фото (инструмента или дефекта).
    2. Бот скажет название и даст совет.
*   **Жалоба:**
    1. Напишите: *"Привезли битую плитку!"*.
    2. Бот пришлет черновик ответа для менеджера.

---

## 5. Развертывание (Developer Guide)

### Локальный запуск:
```bash
# 1. Клонирование
git clone https://github.com/your-repo/construction-ai.git
cd ConstructionAI_System

# 2. Настройка
cp .env.example .env
# Впишите API ключи в .env

# 3. Запуск (через скрипт)
./start.sh
```

### Запуск в Docker:
```bash
docker-compose up -d --build
```

### Структура проекта:
*   `src/llm_client.py`: Логика общения с нейросетями (Failover).
*   `src/prompts.py`: Библиотека промтов.
*   `src/visualizer.py`: Генератор графиков.
*   `telegram_bot.py`: Точка входа (UI).

---

## 6. План развития (Roadmap)

1.  **Q1 2025:** Интеграция с 1С / Bitrix24 (Webhook).
2.  **Q2 2025:** Голосовой ассистент (Whisper API) для приема заявок от прорабов за рулем.
3.  **Q3 2025:** Векторная База Знаний (RAG) по всей номенклатуре товаров (100k+ SKU) для мгновенных консультаций.

---
*Created by Gemini CLI Agent & [Your Name]*
